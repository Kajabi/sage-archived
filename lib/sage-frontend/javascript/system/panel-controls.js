Sage.panelControls = (() => {
  // ==================================================
  // Variables
  // ==================================================


  // ==================================================
  // Functions
  // ==================================================

  const init = (el) => {
    console.log('found element', el);
  }

  const unbind = () => {
    // TODO
  }

  return {
    init: init,
    unbind: unbind,
  };
})();
