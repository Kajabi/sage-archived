<!DOCTYPE html>
<html lang="en">
  <head>
    <%= render "application/meta" %>
    <%= render "application/styles" %>
  </head>
  <body class="sage-app sage-docs">
    <%= render "application/content_skip_link",
      link_id: "main-content"
    -%>
    <header class="sage-app__header sage-header">
      <%= render "application/assistant",
        menu_button: true
      -%>
    </header>
      <%= render "application/sidebar" %>
      <main class="sage-app__stage" id="main-content">
        <div class="sage-container sage-container--xl">
          <div class="sage-grid-old-row">
            <div class="sage-grid-old-col--lg-9 sage-type">
              <%= yield :heading %>
              <hr/>
            </div>
          </div>
          <div class="sage-grid-old-row">
            <div class="sage-grid-old-col--lg-9">
              <%= yield %>
            </div>
            <div class="sage-grid-old-col--lg-3 sage-grid-old-col--md-hide"><%= yield :quick_links %></div>
          </div>
        </div>
      </main>
      <div class="sage-overlay" aria-hidden="true"></div>
    </div>
    <%= render "scripts" %>
  </body>
</html>
