<ul class="sage-indicator-list">
  <% for i in 1...(num_items + 1) %>
    <li class="sage-indicator<%= " sage-indicator--current" if current_item == i %>">
      <span class="visually-hidden">
        <%= "#{label} #{i}" %>
      </span>
    </li>
  <% end %>
</ul>