<h4>As a Panel list</h4>
<div class="sage-panel__list">
  <%= sage_component SageSortable, { resource_name: "link" } do %>

    <% 3.times do %>
      <%= sage_component SageSortableItem, {
        title: "Name Text",
        subtitle: "Label Text",
        url_update: "update/endpoint",
        id: "id"
      } do %>
        <%= sage_component SageStatusIcon, {
          icon: "lock",
          value: "Authenticated Only"
        } %>
        <%= sage_component SageButton, {
          attributes: {
            "data-js-tooltip": "Preview"
          },
          style: "secondary",
          subtle: true,
          icon: { name: "preview-on", style: "only" },
          value: "Preview"
        } %>
        <%= sage_component SageButton, {
          attributes: {
            "data-js-tooltip": "Edit",
          },
          style: "secondary",
          subtle: true,
          icon: { name: "pen", style: "only" },
          value: "Edit"
        } %>
      <% end %>
    <% end %>

    <%= sage_component_section :empty do %>
      <%= sage_component SageEmptyState, {
        title: "Empty State Title",
        text: "Empty state text",
      } %>
    <% end %>
  <% end %>
</div>

<h4>As cards</h4>
<%= sage_component SageSortable, { resource_name: "link" } do %>
  <% 3.times do %>
    <%= sage_component SageSortableItem, {
      title: "Name Text",
      subtitle: "Label Text",
      url_update: "update/endpoint",
      id: "id",
      card: true
    } do %>
      <%= sage_component SageStatusIcon, {
        icon: "lock",
        value: "Authenticated Only",
      } %>
      <%= sage_component SageButton, {
        attributes: {
          "data-js-tooltip": "Preview"
        },
        style: "secondary",
        subtle: true,
        icon: { name: "preview-on", style: "only" },
        value: "Preview"
      } %>
      <%= sage_component SageButton, {
        attributes: {
          "data-js-tooltip": "Edit",
        },
        style: "secondary",
        subtle: true,
        icon: { name: "pen", style: "only" },
        value: "Edit"
      } %>
    <% end %>
  <% end %>

  <%= sage_component_section :empty do %>
    <%= sage_component SageEmptyState, {
      title: "Empty State Title",
      text: "Empty state text",
    } %>
  <% end %>
<% end %>
