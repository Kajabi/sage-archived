<%= content_for :heading do %>
<%= md('
# Typography

Sage provides a constrained, purposeful set of typographic styles.
These styles map as much as possible to functional roles so you know when each can be used.
') %>
<% end %>
<%= content_for :quick_links do %>
  <div class="quick-links">
    <h2 class="quick-links__title">Contents</h2>
    <ul class="quick-links__body">
      <li class="quick-links__item">
        <a href="#guidance" class="quick-links__link">Guidance</a>
      </li>
      <li class="quick-links__item">
        <a href="#spec-table" class="quick-links__link">Type Spec Reference</a>
      </li>
    </ul>
  </div>
<% end %>
<%= md('
The Sage type system uses a major second modular scale (factor of 1.125) with a base font size of 16px.

Typography in our system uses `t-sage-` classes in order to apply a given typographical specification to an element
These can be used on any element to apply the desired typographical styles regardless of the tag. This allows a
wide range of flexibility for applying a desired style to an element while still using the most semantic element possible
for a given context.

Setting individual styles manually should be avoided, but where needed, should be sure to use the appropriate
[design tokens](/pages/foundations/token).
In the end, we prefer adding new entries to the type spec over including one-off styling.
', true) %>
<%= sage_component SagePanel, { clear_top_padding: true, spacer: { top: :lg, bottom: :lg }} do %>
  <%= sage_component SageTable, {
    headers: [
      "Type Spec",
      "Class",
      "Weight",
      "Size",
      "Line-height",
      "Kerning",
    ],
    rows: [
      {
        style: %(<span class="t-sage-heading-1">Heading 1</span>),
        class: md('`t-sage-heading-1`'),
        weight: "700",
        size: "29px",
        line_height: "36px",
        kerning: "0.3px",
      },
      {
        style: %(<span class="t-sage-heading-2">Heading 2</span>),
        class: md('`t-sage-heading-2`'),
        weight: "700",
        size: "26px",
        line_height: "32px",
        kerning: "0.3px",
      },
      {
        style: %(<span class="t-sage-heading-3">Heading 3</span>),
        class: md('`t-sage-heading-3`'),
        weight: "700",
        size: "23px",
        line_height: "32px",
        kerning: "0.3px",
      },
      {
        style: %(<span class="t-sage-heading-4">Heading 4</span>),
        class: md('`t-sage-heading-4`'),
        weight: "600",
        size: "20px",
        line_height: "28px",
        kerning: "0.3px",
      },
      {
        style: %(<span class="t-sage-heading-5">Heading 5</span>),
        class: md('`t-sage-heading-5`'),
        weight: "600",
        size: "18px",
        line_height: "24px",
        kerning: "0.3px",
      },
      {
        style: %(<span class="t-sage-heading-6">Heading 6</span>),
        class: md('`t-sage-heading-6`'),
        weight: "600",
        size: "16px",
        line_height: "20px",
        kerning: "0.3px",
      },
      {
        style: %(<span class="t-sage-body">Body*</span>),
        class: md('`t-sage-body`'),
        weight: "400*",
        size: "16px",
        line_height: "28px",
        kerning: "0.2px",
      },
      {
        style: %(<span class="t-sage-body-small">Small Body*</span>),
        class: md('`t-sage-body-small`'),
        weight: "400*",
        size: "14px",
        line_height: "24px",
        kerning: "0.2px",
      },
      {
        style: %(<span class="t-sage-body-xsmall">Extra Small Body*</span>),
        class: md('`t-sage-body-xsmall`'),
        weight: "400*",
        size: "12px",
        line_height: "20px",
        kerning: "0.2px",
      }
    ],
  } %>
  <p class="t-sage-body-xsmall">
    * The <code>t-sage-body{-size}</code> classes also have weight variations:
    append <code>-med</code> for <span class="t-sage-body-xsmall-med">medium (500)</span>;
    append <code>-semi</code> for <span class="t-sage-body-xsmall-semi">semibold (600)</span>;
    append <code>-bold</code> for <span class="t-sage-body-xsmall-bold">bold (700)</span>
  </p>
<% end %>
<%= md('
The type specs above map logically to corresponding HTML elements, as well as the following:

- <span class="t-sage-body">Body</span> is used as the default type style, and is thus applied to elements such as
    `li`, `dd`, `td`, `blockquote`, and more.
- <strong>Body Bold</strong> is used for elements that are typically bolded such as
    `tr`, `dt`, `strong`, and `b`.

As an example, see here how the "Heading 3" type spec can be applied to an
`h3` element:
', true) %>
  <div class="docs-container">
    <div class="sage-type">
      <h3 class="t-sage-heading-3">Lorem ipsum dolor sit</h3>
      <div class="sage-code-snippet">
        <pre class="prettyprint"><code>&lt;h3 class="t-sage-heading-3"&gt;Lorem ipsum dolor sit&lt;/h3&gt;</code></pre>
      </div>
    </div>
  </div>
  <p class="t-sage-body">
    But the exact specs can also be applied to any element such as a `span`.
    This is not meant to be an excuse for using non-semantic markup, but rather,
    to free a spec to be applied to the most semantic element in context regardless
    of the spec's name.
  </p>
  <div class="docs-container">
    <div class="sage-type">
      <span class="t-sage-heading-3">Lorem ipsum dolor sit</span>
      <div class="sage-code-snippet">
        <pre class="prettyprint"><code>&lt;span class="t-sage-heading-3"&gt;Lorem ipsum dolor sit&lt;/span&gt;</code></pre>
      </div>
    </div>
  </div>
  <p class="t-sage-body">
    For setting text in open areas where simpler markup is desired, or where a
    content management system controls the markup being output, we recommend applying
    the `sage-type` class on a container around such content. This
    leads to some default styling applied to the tags within the container
    include our preferred colors and block spacing.
  </p>
  <div class="docs-container">
    <div class="sage-type">
      <div class="sage-type">
        <h3>User-generated content</h3>
        <p>See how content inside this <code>sage-type</code> container automatically recieves corresponding type specs?</p>
      </div>
      <br />
      <div class="sage-code-snippet">
        <pre class="prettyprint"><code>&lt;div class="sage-type"&gt;
      &lt;h3&gt;User-generated content&lt;/h3&gt;
      &lt;p&gt;See how content inside this &lt;code&gt;sage-type&lt;/code&gt; container automatically recieves corresponding type specs?&lt;/p&gt;
    &lt;/div&gt;</code></pre>
      </div>
    </div>
  </div>

  <%= md('## Coloring Type

    While applying colors to type on-the-fly should generally be avoided
    we do provide the following utility classes for such cases where its most practical:
  ', true) %>

<%= sage_component SagePanel, { clear_top_padding: true, clear_bottom_padding: true, spacer: { top: :lg } } do %>
  <%= sage_component SageTable, {
    headers: [
      "Color",
      "Type class",
    ],
    rows: [
      {
        color: md('<span class="t-sage-body-semi t-sage--color-primary">Primary</span>'),
        type_class: md('`t-sage--color-primary`'),
      },
      {
        color: md('<span class="t-sage-body-semi t-sage--color-charcoal">Charcoal</span></td>'),
        type_class: md('`t-sage--color-charcoal`'),
      },
      {
        color: md('<span class="t-sage-body-semi t-sage--color-grey">Grey</span></td>'),
        type_class: md('`t-sage--color-grey`'),
      },
      {
        color: md('<span class="t-sage-body-semi t-sage--color-red">Red</span></td>'),
        type_class: md('`t-sage--color-red`'),
      },
      { 
        color: md('<span class="t-sage-body-semi t-sage--color-orange">Orange</span></td>'),
        type_class: md('`t-sage--color-orange`'),
      },
      {
        color: md('<span class="t-sage-body-semi t-sage--color-yellow">Yellow</span></td>'),
        type_class: md('`t-sage--color-yellow`'),
      },
      { 
        color: md('<span class="t-sage-body-semi t-sage--color-sage">Sage</span></td>'),
        type_class: md('`t-sage--color-sage`'),
      },
      { 
        color: md('<span class="t-sage-body-semi t-sage--color-purple">Purple</span></td>'),
        type_class: md('`t-sage--color-purple`'),
      }
    ]
  } %>
<% end %>
